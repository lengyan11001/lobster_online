# 2026-06-06 服务器排查与打包交接

> 给下个会话快速接手用。不要把本文件当发布记录；服务器发布、OTA 下发、上传服务器都是独立动作，必须等用户明确说。

## 当前结论

- 用户刚才说服务器不可访问，后确认是用户本地网络挂了，服务器侧暂不需要继续处理。
- 本轮没有发布服务器，没有上传 OTA，没有推包。
- `https://bhzn.top/api/health` 本地测试返回 200：
  - `{"status":"ok","lan_ip":"10.1.0.14","mcp":{"reachable":true,"tools_count":14}}`
- `http://bhzn.top/api/health` 也返回 200。
- `bhzn.top` DNS 解析到 `42.194.209.150`。
- `bhzn.top` 的 443、80、22 端口本地 `Test-NetConnection` 都是通的。
- `https://bhzn.top/` 和 `https://bhzn.top/index.html` 返回 404；这不代表 API 挂了，可能只是服务器根路径没有配置静态首页。
- 还没 SSH 上服务器继续看日志；当时 PowerShell 里 `bash` 不在 PATH，`bash scripts/ssh_run_remote.sh ...` 没跑起来。

## 重要规则

- 用户多次强调：打包和发布服务器是两件事。
- 只有用户明确说“发布/部署服务器/发服务器”才部署。
- 用户说“打包”时默认只本地打包，不上传服务器。
- 用户明确说过：前面误发的 OTA 需要撤掉；以后不要自作主张发服务器。
- 发布服务器走仓库既有发布流程，不要用 MCP。
- `lobster_online` 不整体上传服务器；服务器代码在 `D:\lobster_server`。
- 不要把 `.env`、`.env.deploy`、token、密码写进文档或提交。

## 本地仓库状态

当前主要工作区：`D:\lobster_online`

当前有未提交改动，下一会话不要随便回滚：

```text
 M CLIENT_CODE_VERSION.json
 M backend/app/api/cutcli_templates_local.py
 M backend/app/api/skills.py
 M pack_full_project.sh
 M scripts/check_client_code_update.py
 M scripts/pack_client_code_ota.py
 M scripts/pack_full_project_zip.py
 M static/client_version.json
 M static/css/index.css
 M static/js/cutcli-template-studio.js
 M static/js/skill.js
?? scripts/ppt_runtime_wheels/
?? scripts/verify_cutcli_preview_style_consistency.py
```

这些改动里有些是前面模板、技能商店、PPT 依赖、打包清理相关工作。接手时先看 `git diff`，不要直接 `reset`。

## 已打包产物

完整依赖包：

```text
D:\lobster_online_完整项目包_bihuo_20260605_191456.zip
size: 1,720,890,471 bytes
sha256: E689830DF02C4C88EC64417C6304B7FB568B0015F39C6332173C3EA732786A8A
```

说明：

- 完整包仍约 1.7G，主要是实际运行依赖：`nodejs`、`browser_chromium`、`deps`、`python`、`desktop/webview2`。
- 已检查不是临时文件撑大的。

带 PPT 依赖的 OTA：

```text
D:\lobster_online_client_code_ota_with_ppt_runtime_20260605210454.zip
size: 216,564,742 bytes
sha256: 9090921A9213FB99471D12A4BC1F344B259EE4420C316D835D99B8E24C8164BB
version: 1.0.64
build: 99
```

说明：

- 这个 OTA 包里已确认包含 `scripts/ppt_runtime_wheels/`，共 38 个 PPT 相关 wheel。
- 包内已确认有：
  - `scripts/check_client_code_update.py`
  - `scripts/pack_client_code_ota.py`
  - `CLIENT_CODE_VERSION.json`
  - `static/client_version.json`
  - `scripts/ppt_runtime_wheels/svglib-1.6.0-py3-none-any.whl`
  - `scripts/ppt_runtime_wheels/cssselect2-0.9.0-py3-none-any.whl`
  - `scripts/ppt_runtime_wheels/tzdata-2026.2-py2.py3-none-any.whl`
- OTA 内没有 `.pyc/.log/.tmp` 这类临时文件。

## PPT OTA 实现点

文件：`D:\lobster_online\scripts\pack_client_code_ota.py`

- 新增 `--with-ppt-runtime-deps` 参数。
- 参数开启时从 `deps/wheels` 复制 PPT 运行所需 wheel 到 `scripts/ppt_runtime_wheels`。
- 这次实际复制了 38 个 wheel。
- 普通 OTA 默认仍不会把大范围 `deps/` 带出去。

文件：`D:\lobster_online\scripts\check_client_code_update.py`

- 新增 PPT runtime 离线安装逻辑。
- 当 OTA 应用内容涉及：
  - `requirements.txt`
  - `skills/ppt_master`
  - `skills/ppt-master`
  - `scripts/ppt_runtime_wheels`
  - `deps/wheels`
- 会调用当前 Python：

```text
python -m pip install --no-index --find-links scripts/ppt_runtime_wheels --find-links deps/wheels ...
```

- 目标是 OTA 应用后离线补齐 PPT Master 运行依赖。

已做过 dry-run：

```text
python\python.exe -m pip install --dry-run --ignore-installed --no-index --find-links scripts\ppt_runtime_wheels ...
```

最终通过。

## 服务器相关

服务器仓库：`D:\lobster_server`

常见入口：

- `https://bhzn.top/api/health`
- `https://bhzn.top/client/client-code/manifest.json`

本轮测试结果：

```text
https://bhzn.top/api/health                         200
http://bhzn.top/api/health                          200
https://bhzn.top/client/client-code/manifest.json   200
https://bhzn.top/auth/me                            401, 未登录正常
https://bhzn.top/skills/skill-store-admin           401, 未登录正常
https://bhzn.top/api/cutcli/templates               401, 未登录正常
https://bhzn.top/                                  404
https://bhzn.top/index.html                         404
```

如果下一会话还要 SSH 排查，优先用 `D:\lobster_server\scripts\ssh_run_remote.sh`，但当前 PowerShell 没有 `bash` 命令，需要用 Git Bash/配置 PATH，或改用已有 Python/Paramiko 运维脚本。

建议远端只读诊断命令方向：

```bash
systemctl is-active nginx lobster-backend lobster-mcp lobster-background lobster-h5 postgresql
systemctl --failed --no-pager
ss -ltnp | grep -E ':(80|443|8000|8001|8002|5432)'
df -h
sudo nginx -t
journalctl -u lobster-backend --since '30 minutes ago' --no-pager -n 100
sudo tail -100 /var/log/nginx/error.log
sudo tail -100 /var/log/nginx/access.log
```

## 下个会话接手建议

1. 如果只是确认服务器是否可访问，先测：

```powershell
Invoke-WebRequest https://bhzn.top/api/health -UseBasicParsing -TimeoutSec 15
Invoke-WebRequest https://bhzn.top/client/client-code/manifest.json -UseBasicParsing -TimeoutSec 15
```

2. 如果用户要求发包，先确认是：
   - 只打包；
   - 打包并上传 OTA；
   - 发布服务器；
   - 还是完整依赖包。
3. 如果继续处理 PPT OTA，优先复查 `scripts/ppt_runtime_wheels` 是否 38 个 wheel，以及更新器的离线安装逻辑。
4. 如果继续处理模板/字幕，注意用户核心诉求是“预览和成片样式一致”“模板配置落库从服务器下发”“客户端不要写死模板”。
5. 如果处理技能商店，用户希望服务器返回什么就展示什么，服务器按用户类型判断；不要客户端拿全量列表再复杂过滤。
